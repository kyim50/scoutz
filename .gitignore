# macOS
.DS_Store

# Secrets — each project also ignores its own, this is a backstop
.env
.env.local
.env.*.local

# Dependencies / build output
node_modules/
dist/
*.log

# Playwright MCP scratch output from local page checks
web/.playwright-mcp/
